# Only install package versions that have been public for at least this many
# days, so a compromised release has time to be caught and unpublished before
# it can reach this project. Raising this may block a same-day security fix;
# use `--min-release-age=0` on a one-off install when that trade-off is worth it.
min-release-age=7
