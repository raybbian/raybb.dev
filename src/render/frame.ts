// Open-water clear colour, per theme. html's background is kept in sync
// (globals.css) so there's no pre-paint flash. Light = origin/main's
// original teal (#2f8a86); dark = a deeper teal.
export const BG_DARK: [number, number, number, number] = [0.11, 0.37, 0.36, 1];
export const BG_LIGHT: [number, number, number, number] = [0.184, 0.541, 0.525, 1];

export const RENDER_MSAA = 4;
